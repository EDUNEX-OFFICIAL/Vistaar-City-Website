# Vistar City — environment (local / Docker prod)

# Lead API — stub mode until dedicated DB is connected.
# LEAD_PERSIST=false

# Future: Postgres on dedicated Vistaar VPS or ERP vistaar tenant
# DATABASE_URL=postgresql://vistar_city:***@host:5432/vistar_city

# Public site URL — used for metadataBase, Open Graph, and JSON-LD (set at deploy).
# NEXT_PUBLIC_SITE_URL=https://vistaarcity.edunexservices.in

# Do not use EDUNEX ERP erp-postgres or tenant vistaar without explicit migration plan.
